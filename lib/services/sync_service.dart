/// SYNC SERVICE STUB (Firebase Disabled)
/// This is a stub service - all methods are no-op
/// Firebase sync is disabled for stable Android builds

class SyncService {
  /// Sync method (no-op)
  Future<void> sync() async {
    // Firebase disabled - no sync
  }

  /// Sync hatim progress (no-op)
  Future<void> syncHatimProgress() async {
    // Firebase disabled - no sync
  }

  /// Sync settings (no-op)
  Future<void> syncSettings() async {
    // Firebase disabled - no sync
  }

  /// Sync reading progress (no-op)
  Future<void> syncReadingProgress(int page) async {
    // Firebase disabled - no sync
  }
}
