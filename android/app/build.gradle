plugins {
    id "com.android.application"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

apply plugin: 'kotlin-android'
// apply plugin: 'com.google.gms.google-services' // FIREBASE DISABLED

android {
    namespace = "com.pocketkhatm.app"
    compileSdk = 34
    ndkVersion = "26.1.10909125"  // Pinned for GitHub Actions compatibility

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId = "com.pocketkhatm.app"
        minSdkVersion = flutter.minSdkVersion
        targetSdk = 34
        versionCode = flutter.versionCode
        versionName = flutter.versionName
        multiDexEnabled true
    }

    buildTypes {
        debug {
            minifyEnabled false
            shrinkResources false
        }
        release {
            minifyEnabled false
            shrinkResources false
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    // FIREBASE TEMPORARILY DISABLED FOR STABLE BUILD
    // implementation platform('com.google.firebase:firebase-bom:33.0.0')
    // implementation 'com.google.firebase:firebase-auth'
    // implementation 'com.google.firebase:firebase-firestore'
    
    // Kotlin stdlib 1.8.22 (safe version)
    implementation "org.jetbrains.kotlin:kotlin-stdlib:1.8.22"
}
